# Plan changelog

Record here every place where implementation reality differed from the plan
documents, and every locked decision that had to change, with the reason.
Newest first.

| Date | Task | What differed | What was done |
|---|---|---|---|
| 2026-09-19 | M5-T05 | `06` §5 has the Everyday "Ask a question" box running **direct mode** with a `read_only_intent` flag, alongside Developer's skip-planning. Direct mode's whole job is to write, and the flag as described changes nothing that would stop it | A third `TurnMode`, `Ask`, rather than a flag on `Direct`. It sends one prompt (`prompts/ask.md`), shuts writes at the engine, selects the engine's read-only mode, and puts the plan gate in front of every permission request — the same three locks the plan phase uses, because an intent that rests on the prompt alone is a wish. There is no execute phase after it. The audit rows say `phase: "asking"`, and the bypass detector that watches the plan phase for a mutation reaching `completed` without asking now watches this phase too. Everyday's second button is `Ask`; Developer's "Run" stays `Direct`, where the words say what that means. `--ask` on `eavery-cli prompt` drives the same path. |
| 2026-09-19 | M5-T02 | §2 asks the check to cover string literals and JSX text in the screens, which proves the engine's words live in `vocab/`. It does not prove they are kept out of what Everyday mode actually renders — the dictionary is exempt from its own rule, so `everyday: "Go back to the last commit"` would pass | The check has a second half: every `everyday:` rendering, and every `same(...)` (which puts one string in both modes), is held to the same word list; `developer:` is exempt, since naming the machinery is what it is for. This is what makes M5-T06 a rule rather than a one-off review. The first half also skips module specifiers and `className`, which are code that happens to be a string and never reach the screen as words. |
| 2026-09-19 | M5-T04 | §3 asks the Documents pane for a tree of the folder; no command returned one, and `03-architecture.md` §7 has none | `eavery-core::documents::list` plus a `list_documents` command. It returns names, relative paths (always `/`-separated, so they compare equal to the digest's) and sizes, and never reads a file: v1 renders no contents, it hands the path to the OS. Capped at 2,000 entries and 8 levels, reporting `truncated` rather than stopping quietly, because a Project may hold up to `MAX_FILES` files. The directories the Journal refuses to walk are skipped, and symlinks are listed but never followed. |
| 2026-09-19 | (none) | The CLI's `a_crashed_engine_fails_the_command_and_prints_its_stderr` had been failing intermittently — two runs in six on the commit before this one — and would have made CI red at random | `wait_loop` in `eavery-acp::conn` waited for the **stderr** reader to reach EOF before failing the pending requests, but not for the stdout reader. An engine that writes a message and exits in the same breath therefore lost it: the request failed with "the engine exited" while the message was still unparsed in the pipe, and the transcript showed the crash without the words leading up to it. Both readers now signal, and the waiter joins them under the one grace period (`DRAIN_GRACE`, was `STDERR_DRAIN_GRACE`). Fifteen consecutive green runs after the change. |
| 2026-09-18 | M3-T01 (CI) | The Windows CI job has been red since the PR #6 merge: the desktop IPC test binary exits 0xc0000139 (`STATUS_ENTRYPOINT_NOT_FOUND`) before running a line. Two earlier diagnoses — the `cdylib` beside the test binaries, then the WebView2 loader — were wrong; the crate-type change stays for its own reasons, and the loader copy step turned out to make no difference | The loader was made to say what it could not find (`.github/scripts/unresolved_imports.py`, run on failure): `TaskDialogIndirect`, which the Wry runtime's dialogs (`rfd`, `tauri-runtime-wry`) import from `comctl32.dll`. That export exists only in Common Controls version 6, and Windows binds a binary to version 6 only when its application manifest asks for it. `tauri-build` embeds such a manifest into the app binary alone; the IPC test binary links the Wry runtime too (`AppCore` holds an `AppHandle`) and had no manifest, so it got version 5. `build.rs` now links every test binary with `windows/common-controls.manifest`, the same dependency `tauri-build` writes, through `rustc-link-arg-tests`. The lib's and the bin's unit-test binaries never imported it — the harness replaces `main`, so nothing referenced the runtime — which is why they started and the IPC test did not. |
| 2026-09-18 | M3-T01 (CI) | The step from 2026-09-18 that copies `WebView2Loader.dll` beside the Windows test binaries copied the first one it found | `webview2-com-sys` unpacks one per architecture in name order — `arm64` first — so the x64 runner got the arm64 loader. The step now picks the DLL under the directory named for the runner's own architecture. It was not the cause of the red job (above), and may not be needed at all: the test binaries import nothing from it that the loader could not find. Kept, because it is cheap and the day an engine build does import it is not the day to find out. |
| 2026-09-18 | M4-T05 | `06` §2.4 has the approval as a UI step and `03-architecture.md` §7 has `approve_plan` / `reject_plan` as commands; nothing says how the turn engine, which holds the turn open, hears the answer | `TurnCallbacks` gained `approval`, a handler like `permission`: the turn engine hands it the `PlanReview` (turn, plan, vendor) and waits, with no timeout, because the only thing a timeout could do is answer for the person. The desktop resolves it through a `PlanDesk` keyed by turn, which `approve_plan` / `reject_plan` answer; the CLI answers from `--approve` / `--edits` or the terminal, and with no terminal says no. `cancel` while the plan waits resolves the wait rather than sending `session/cancel`, since there is no prompt in flight; the turn ends `Cancelled` and `TurnFinished` says `plan_rejected` rather than `cancelled`, so the transcript can say "nothing was changed" instead of "stopped". |
| 2026-09-18 | M4-T05 | `06` §2.1 reads the mode hints off `EngineSpec`, which lives in `eavery-engines`, and `eavery-core` cannot depend on it | `EngineFacts` (vendor, the two hints, the exit signatures) in `eavery-core::engine`, filled by `EngineSpec::facts()` and passed to `ProjectRunner::open`; `pick_mode` moved to core with it, re-exported from `eavery-engines`. The work mode is the asking-mode hint's match or else the mode the session opened in, and a switch is only sent when the engine is not already there, tracking `current_mode_update` on the way. |
| 2026-09-18 | M4-T05 | `06` §2.4's plan card needs the vendor, and the frontend would have had to run every engine's health check to look it up | `CoreEvent::PlanReady` carries `vendor` next to the plan. It is a fact about that turn — who saw the documents to make this plan — and putting it on the event means a transcript replayed later says the same thing it said at the time. |
| 2026-09-18 | M4-T05 | `06` §5 says direct mode sends the execute prompt with the request where the plan goes; M2-T07's direct mode sent the request bare | Now as §5 says, through `execute_prompt_for`. The fake-agent scripts match on substrings of the request, which the prompt still contains, so nothing else moved. |
| 2026-09-18 | M4-T05 | `06` §2.2's bypass rule says "a `tool_call` with kind edit/delete/move reaches `completed` during planning without a permission request" | The turn engine keeps the tool-call ids that asked during planning and reports a completed mutation that is not among them, once per call, as `Error { PlanGateBypassed }` with a next action in Everyday words; the plan phase carries on and the post-turn checkpoint and the digest have whatever it changed. A mutation that asked and was refused, then reported itself completed, is not a bypass. |
| 2026-09-18 | M4-T03 | `06` §2.2 has a `PlanGateHandler`, and its `fs/write_text_file` refusal needs the turn engine to reach the ACP layer's `FsGuard`; the turn engine holds an `Arc<dyn Engine>` and cannot | `Engine` gained `set_writes_allowed(bool)`, a no-op by default (an engine that never writes through the client has nothing to close); `AcpEngine` flips its guard. The gate itself is `policy::plan_gate`, a pure function of the call and the engine's `plan_exit_signatures`, with `is_plan_exit` and `bypassed_plan_gate` (the §2.2 last paragraph) beside it; the turn engine applies them once M4-T05 gives it a plan phase to apply them in. The fake agent's `fs_write` gained `expect_refused`, like `fs_read` already had, so a planning script asserts the refusal from its own side and a gate that opened would fail the test from both ends. |
| 2026-09-18 | M4-T02 | `06` §3.1 says to handle the Windows verbatim prefix in `policy::is_inside`, and `CHANGELOG` of 2026-09-15 says that function should call `paths::is_inside` | The handling went into `paths::is_inside`, which `policy::is_inside` re-exports: the ACP layer's own inside-the-Project check uses the same function and gets the same fix. A `\\?\` prefix is stripped from both sides before anything else — canonicalisation cannot touch a path that does not exist yet — and two Windows-shaped paths are compared component-wise, either separator, ignoring case, on every OS, which is what lets the `\\?\C:\` root case §3.1 asks for run on Linux and macOS CI too. |
| 2026-09-18 | M4-T02 | §3.1 gives `classify(call: &ToolCallView, ...)`, and §3.2's last column is for the dialog to apply | `classify` takes a borrowed `CallFacts` (kind, title, locations, `rawInput`), so one function serves a permission request and a tool-call update, and `ToolCallView` has no `rawInput` to match Connector names against. `PermissionView` carries the table's answer rather than leaving the dialog to re-derive it: `always` (`never` / `yes` / `developer_only`, the mode applied in the UI), `in_plan` for an Outbound call, `connector`, and `raw_input`. The core enforces the column as well as offering it: an `AllowAlways` the row forbids is narrowed to `AllowOnce` before it is remembered or sent to the engine, so the rule does not depend on the frontend. "Always" (§3.3) is stored per Project under the settings key `always_allow:<project_id>` as a list of signatures (`kind|connector|normalised title`), and the table is applied again at lookup, so an "always" given for a command in Developer mode does not carry into Everyday mode, where it was never offered. |
| 2026-09-18 | M4-T02 | `ProjectRunner::open` took the Connectors as `&[McpServerSpec]` | It takes a `ConnectorRegistry`: each entry is the spec plus the `outbound` flag and the tool names the policy matches on, and the runner hands `specs()` to `session/new`. What fills the registry is M6-T08; until then every caller passes an empty one, as before. |
| 2026-09-18 | M4-T01 | §4 asks for `render(template, &HashMap)` and gives the two templates | As written, plus the callers the templates need and §4 does not name: `plan_prompt`, `execute_prompt`, and `execute_prompt_for`, which is direct mode's "the request where the plan goes" (§5); and `plan_as_text`, because the execute prompt wants the plan as prose and nothing says what that prose is. A key with no value renders as nothing rather than as a literal `{{key}}`, which a model would read as an instruction of unknown meaning; the playbooks line says `(none)` when there are none, so it does not read as a list cut short. |
| 2026-09-18 | M4-T04 | §2.3's parser rules | As written, with four things the rules do not say: a block whose JSON has no `summary` takes the first line of the reply; a fence may be indented up to three spaces or be longer than three backticks, as CommonMark allows and some engines emit; list items inside any other fenced block are not steps; and a task-list checkbox is stripped from a step. Parsing never fails and never logs above `warn`. |
| 2026-09-18 | M3-T06 | `11-testing-ci.md` §2 gives the fake agent its script through `--script`, and the desktop app starts every engine the same way, with no per-engine arguments | The fake agent also reads `EAVERY_FAKE_SCRIPT`, so the window can be tried end to end without a real assistant (`apps/desktop/README.md`). That is how the M3 screens were checked here: a folder opened through the picker, a turn with the `hello.json` script, the permission auto-allowed by policy, the Digest, Undo, and Redo — on a virtual display, with the fake engine picked in Settings. Nothing in the app knows about the variable; it is the child's own environment, inherited. |
| 2026-09-18 | M3-T08 | `03-architecture.md` §8 has `logs/eavery.log` "rotated", and §9 says nothing about how the panel reads it | `tracing` now writes to stderr and to that file (`eavery_core::diagnostics::open_log`), and the file is rotated by one rename at startup when it is over 10 MB — `eavery.log` becomes `eavery.log.1`, kept once. No `tracing-appender`: its rolling appender rotates by time, not size, and adds a background thread and a dependency for what a rename does. The panel reads the tail through a new `diagnostics` command, which reads the last lines from the end of the file rather than the whole of it, and answers on a fresh install with no log at all — the panel is for when something already went wrong and must not be the next thing that does. |
| 2026-09-18 | M3-T07 | `05-git-journal.md` §6 says Redo is `restore(turn.post_checkpoint)` | Redo goes to the checkpoint immediately below the restore commit in the list. That is D16's "Before going back" checkpoint when there was anything to keep, and the previous head when there was not — and it is the only target that holds the user's own edits made after the turn, which `post_checkpoint` by definition does not. The label says where it goes. Undo is unchanged: the last finished turn's `pre_checkpoint`, from the Digest, the History tab, and Cmd/Ctrl+Z outside a text field. |
| 2026-09-18 | M3-T07 | `07-ui-vocabulary.md` §3 says the permission dialog offers "always" only where the decision table allows, and the table is M4-T02 | Until then: "Always allow" is offered for `read`, `reversible` and `execute`, never for `outbound` or `destructive`, and only when the engine offered that option. Esc answers `reject_once`: a question dismissed is a question answered no. M4-T02's table replaces the three-line rule in `PermissionDialog.offered`. |
| 2026-09-18 | M3-T06 | `07-ui-vocabulary.md` §3 gives the Documents pane a folder tree, and §7 has no command that lists a Project's files | The pane shows what the core can already answer: the folder (with "Show folder"), the files the last turn added, changed and removed — each opens with the OS default app — and the Not-protected list. The tree, with its per-file markers, is M5-T04 as planned, and will need a `list_files` command that walks with the Journal's exclusion rules rather than the raw folder, so that what it shows is what Undo covers. |
| 2026-09-18 | M3-T06 | §3 has "Plan it" as the composer's primary button | It is there, and disabled with a tooltip that says why, until M4-T05 makes `mode: "plan"` do something. "Ask" is primary meanwhile. Hidden-not-removed (§4, rule 1) applies to the button as much as to Developer elements. |
| 2026-09-18 | M3-T06 | `03-architecture.md` §7's command table | Two commands added: `journal_info` (`{ path, size_bytes, loose_objects }`, for the Home screen and the Files pane in Developer mode) and `diagnostics` (`{ version, data_dir, log_path, log_tail }`, for M3-T08). `journal_size` stays as it was. Both are in the IPC test and the bindings. The dialog plugin (`tauri-plugin-dialog`) is in for the folder picker, as §1 of `07` plans; `opener:allow-open-path` is scoped to any path, because the user's documents may be anywhere. |
| 2026-09-18 | M3-T06 | `07-ui-vocabulary.md` §1 says CSS modules, `zustand`, and a `t()` that reads the mode from the store | One plain stylesheet (`styles/app.css`): the classes are few and one file is easier to keep consistent between light and dark. No state library, as M3-T05 already decided. `t()` is pure — `t(mode, key, vars)` — and `useT()` is the hook that binds it to the store, because the store itself needs the vocabulary for the messages it writes (locked files after a restore) and a `t()` that read the store would make the two depend on each other. |
| 2026-09-18 | M3-T01 (CI) | The Tauri template gives `src-tauri` `crate-type = ["staticlib", "cdylib", "rlib"]` | Cut to `["lib"]`. The first two are for iOS and Android, neither of which v1 is (`01-implementation-plan.md` §2). On Windows the `cdylib` is a DLL sitting beside the test executables in `target\debug\deps`, and the loader resolves it by name ahead of the test binary's own statically linked copy; it is built without the `tauri/test` feature the tests turn on, so its exports do not match and the test exits with `STATUS_ENTRYPOINT_NOT_FOUND` before running a line. Linux and macOS never noticed. Mobile work brings both crate types back, along with whatever test layout that needs. |
| 2026-09-18 | M3-T02 (CI) | The generated `apps/desktop/src/types.ts` is compared byte for byte against what is committed, and Git for Windows checks text files out as CRLF | `.gitattributes` with `* text=auto eol=lf`, so the working tree is LF on every platform. Nothing stored changed — the repository was already all-LF — but without it the bindings test is stale on Windows and nowhere else, which is a failure about the checkout rather than about the types. Normalising the tree fixes the cause; special-casing the one file would have left the next generated file to find it again. |
| 2026-09-18 | M2-T08 (CI) | The second time a Windows path form has broken a test that passes everywhere else (after the verbatim-path entry of 2026-09-15) | A temp directory on a GitHub Windows runner arrives as a short 8.3 path (`C:\Users\RUNNER~1\AppData\Local\Temp\...`) and resolves to the long one (`C:\Users\runneradmin\...`). The CLI stores and prints the resolved form, which is right — `--project` has to find the Project whichever form was typed — so the tests were wrong to assert that what came back was the string they passed in. The fixture now keeps both: commands are given the folder *as typed*, and assertions are made against the canonical form. That tests more than it did before, because passing the unresolved form is exactly what someone typing a path does. |
| 2026-09-17 | M3-T05 | §7 says the UI detects gaps in `seq` and re-fetches with `list_events`; C13 says `seq` is global and the UI filters by Project | Those two together mean a gap cannot be detected per session: two Projects working at once interleave their events, so consecutive events in one conversation almost never have consecutive `seq`s, and "seq != last + 1" would refetch constantly. The feed tracks the global counter, and when *that* skips, every session being watched asks for everything after the last `seq` it actually holds. Events are held keyed by `seq`, so one that arrives twice — live and again in a refetch — is stored once, and the window's reaction to an event (a permission dialog, a running turn) is keyed by `seq` too rather than by counting, because a refetch can insert events before ones already held. |
| 2026-09-17 | M3-T04 | `03-architecture.md` §7 has `checkpoint_now` and `restore_checkpoint` as commands, with nothing said about whether an engine has to be running | Neither starts one. Both go through the Project's runner when an engine is already going, so they land in the transcript, and otherwise straight at the Journal through `turn::checkpoint_without_session` / `restore_without_session`. "Save a point I can come back to" and "put it back" are not an assistant's doing; making them start one would mean a folder could not be protected until an engine was installed and signed in. The IPC test is what found it: `checkpoint_now` failed with "Codex isn't installed". |
| 2026-09-17 | M3-T04 | §7 says `restore_checkpoint` returns the new restore commit | It returns a `RestoreOutcome`: the checkpoint **and** every file something else held open and so was left alone. `05-git-journal.md` §5 requires those files to be named to the user, and a command that answered with the checkpoint alone would have nowhere to name them. |
| 2026-09-17 | M3-T03 | §7 says the `core://event` payload "includes `seq`" without saying what carries it | The payload is the store's `StoredEvent`: `seq`, `session_id`, `turn_id`, `at`, and the `CoreEvent` itself. That is the row the store already writes, so the event the window gets and the row `list_events` replays are the same shape — which is what makes gap re-fetching a matter of asking for everything after a number. |
| 2026-09-17 | M3-T02 | `03-architecture.md` §2 and M3-T02 expect `ts-rs` to produce one `apps/desktop/src/types.ts` | `ts-rs`'s own export writes one file per type, and `#[ts(export)]` with a shared `export_to` produces a file per type with cross-imports rather than one bundle. The test walks `TS::visit_dependencies` from the twelve types the IPC contract hands the frontend, collects each declaration once, and writes them in name order — so the list that has to be maintained by hand is the IPC surface, not every type. `seq` and the audit `detail` carry `#[ts(type = "...")]`: `u64` would otherwise arrive as a `bigint` that cannot be compared with a number, and `serde_json::Value` has no `TS` impl without another feature. |
| 2026-09-17 | M3-T01 | The Tauri template puts a `[profile.release]` in `src-tauri/Cargo.toml` | Moved to the root manifest, because cargo ignores a profile in a workspace member. Its `panic = "abort"` was dropped: it stops `cargo test --release` compiling at all, and the size it saves is not worth losing the ability to run the tests in release. |
| 2026-09-17 | M3-T01 | `11-testing-ci.md` §5's workflow is Rust only | CI now installs pnpm and Node and runs `pnpm install --frozen-lockfile && pnpm build` in `apps/desktop` before anything cargo does. `tauri::generate_context!` embeds `frontendDist` at compile time, so without a built `dist/` the workspace does not compile — on any of the three OSes. |
| 2026-09-17 | M2-T08 | Nothing in the plan says where `eavery-cli` gets its data directory, and `03-architecture.md` §8 names one fixed location | A global `--data-dir`, then `$EAVERY_DATA_DIR`, then `ProjectDirs::from("dev", "eavery", "Eavery")`. The tests need a data directory that is not the user's, and so does anyone trying the CLI on a copy of a real folder before trusting it with the original. |
| 2026-09-17 | M2-T07 | `03-architecture.md` §6 has restore as a method on the turn engine, which holds an engine | Undo also exists as `turn::restore_without_session`, taking a store and a Journal and no engine at all. `eavery-cli undo` has no conversation open and must not have to start one — Undo is the button that makes everything else safe to try. The one thing the engine-less path does not do is write a `Restored` event, because `events.session_id` is not nullable and there is no session to attach it to. If M3 needs a restore made outside a conversation to appear in a transcript, that column becomes nullable in migration 0002. |
| 2026-09-17 | M2-T07 | C13 says one turn per Project and a restore refused while a turn runs | Held in the turn engine, which is per process. Two `eavery-cli` invocations against the same Project at the same time are not stopped by anything: there is no cross-process lock. That is the CLI's problem rather than the product's — the desktop app is one process — but it is worth knowing before scripting the CLI in a loop. |
| 2026-09-17 | M2-T07 | `06-plan-gate-permissions.md` §3.1 classifies a tool call from its `kind` and `locations`; `PermissionView` carries neither | `PermissionView` gained `kind` and `locations`, and the turn engine reclassifies every permission request before the user is shown it. Without this an edit outside the Project — which Undo cannot reach, and which §3.1 calls `Destructive` — would have been auto-allowed in direct mode as `Reversible`, on the strength of a guess made by the layer that does not know where the Project is. The M4-T02 `policy::classify` replaces the private function that does it now, adding the Connector registry it needs for the `outbound` flag. |
| 2026-09-17 | M2-T06 | `03-architecture.md` §8 gives the schema as seven bare `CREATE TABLE`s with no types enforced, no foreign keys, and the audit table's append-only rule stated as "no UPDATE or DELETE statements exist in the code for it" | The schema carries the rules instead. Every table is `STRICT`, so a column typed `TEXT` refuses an integer rather than handing one back later to a `parse` that cannot fail gracefully. `sessions`, `turns`, `events` and `checkpoints` have foreign keys onto their parents with `ON DELETE CASCADE`, and the store sets `PRAGMA foreign_keys = ON` on every connection — SQLite defaults it off, which would make the cascades decoration. `audit` has no foreign key at all, because a cascade from `projects` would be a DELETE: removing a Project forgets its conversation and keeps its decisions. The append-only rule is two triggers that `RAISE(ABORT)`, tested from a second connection that knows nothing about this crate; a rule that lives only in the callers holds until someone writes a new caller. Timestamps are RFC 3339 in UTC with nine fractional digits, fixed width, so `ORDER BY` on the column is chronological. |
| 2026-09-17 | M2-T06 | Nothing in the plan says how an event finds its `turn_id` on the way into the `events` table | `CoreEvent::turn_id()` was added and `append_event(session_id, event)` reads it off the event rather than taking it alongside: a caller that got the two out of step would write a transcript that cannot be reassembled. Two variants answer `None` — a restore happens between turns, and an engine's status is not about one — and a `CheckpointCreated` takes the turn from the checkpoint it carries, so the event and the checkpoint cannot disagree. |
| 2026-09-16 | M2-T05 | C12 says to pack loose objects above 5,000 with `git2`'s `Odb` / `packbuilder` | Not done, and deliberately left out rather than half-written. Writing a pack is the easy half; the space is only reclaimed by deleting the loose copies once the pack holds them, libgit2 exposes no `gc`, and hand-rolling object deletion against a user's only copy of their documents is not something to invent in passing. `Journal::loose_object_count()` reports the number so Settings can show it and so the design decision is taken with a figure in front of it. Everything else in M2-T05 — `unprotected()`, `size_on_disk()`, the guards, `scan_project` — is done. |
| 2026-09-16 | M2-T03 | `05-git-journal.md` §3 says to decide which deltas get a text diff with `blob.is_binary()` | `git2::Patch::from_diff` does not answer `None` for a binary delta; it produces a patch whose body is "Binary files a/x and b/x differ" and which has no hunks. The rule is therefore "no hunks, no text diff", which also covers a mode-only change and a delta whose patch is not UTF-8. A test on real `.xlsx`-shaped bytes holds it down. |
| 2026-09-16 | M2-T01 | `05-git-journal.md` §3 creates the Journal with `RepositoryInitOptions::workdir_path(root)` | That writes a gitlink file into the Project folder, which §1 forbids outright — a user's `git status` in their own documents folder would answer for Eavery's history — and libgit2 refuses it altogether when the Project is already a git repository ("cannot overwrite gitlink file"), which would leave everyone who works in git unable to have their folders protected. The repository is initialised bare and given its work tree through `core.worktree` instead, with no gitlink. A Project that is already a repository keeps its own `.git`, and a test asserts both that and that Eavery's history excludes it. |
| 2026-09-16 | M1-T05 (partial) | `04-acp-engines.md` §9 step 3 detects "needs sign-in" from a non-empty `authMethods` plus an auth error on `session/new`. The Claude adapter does neither half. | Adapter 0.78.0 reports `authMethods: []` whether or not the user is signed in, and an unauthenticated `session/new` fails with a bare `-32603 Internal error`. The sign-in state arrives instead as an unprompted notification on a vendor-extension method, `_auth/status_update`, with `{"authStatus":{"kind":"none","label":"Not logged in"}}`. Nothing was built on it: a rule derived from the not-signed-in case alone cannot be checked against the signed-in one, and `eavery-acp` is meant to hold no engine-specific knowledge. Health checks report the engine as `Unavailable` with the session error as the developer-facing reason until M1-T05 decides. Full record, including the initialize response (protocol v1, `loadSession: true`, `promptCapabilities.embeddedContext`, `mcpCapabilities` http+sse), in `manual-tests/M1-claude-partial.md`. |
| 2026-09-16 | M1-T03 | `04-acp-engines.md` §9 does not say what happens when an adapter is installed but the CLI it drives is not | Reported as `NeedsSignIn` without spending fifteen seconds on a handshake. `08-onboarding-packaging.md` §2 already says to check for the underlying CLI "because the adapter is useless without a login"; if there is no CLI there is no login, and the handshake can only arrive at the same answer more slowly. `EngineSpec` gained `sign_in_command` for the one command that screen shows verbatim (`07-ui-vocabulary.md` §5), which is not derivable from the other fields: Codex's is `codex login`, Claude's is `claude`. |
| 2026-09-16 | M1-T02 | The plan says to run the login-shell probe and "set it on the Eavery process" | The probed PATH is returned as data and threaded through discovery and into each engine child's environment instead. `std::env::set_var` is `unsafe` in edition 2024 — another thread reading the environment concurrently is undefined behaviour — and every crate here denies unsafe code. Passing the PATH to children is what mutating the process environment was for. `fix-path-env` itself was not used: it is the same probe without a timeout Eavery controls, and §C5 offers the equivalent. |
| 2026-09-16 | M1-T01 | `02-challenges.md` C5 says to use the `which` crate for discovery | Discovery resolves against a `Platform` parameter instead, with its own directory scan. `which` resolves for the host, so the Windows rules — `.exe` before `.cmd`, a `.cmd` shim launched through `cmd /C` — could only ever be tested on Windows, and path bugs that appear on one OS alone are exactly what M0 already lost a CI run to. The scan is about forty lines and every rule is now tested on all three platforms from any of them. |
| 2026-09-15 | M0 (CI) | The Windows verbatim-path hazard `06-plan-gate-permissions.md` §3.1 warns about is not confined to `policy::is_inside`; it bit the ACP layer and the CLI first | `std::fs::canonicalize` returns `\\?\C:\...` on Windows. That path was being used as the session `cwd`, so it went over the wire to the engine, which joined `{{cwd}}/notes.txt` onto it — and in a verbatim path a forward slash is not a separator, so the write landed nowhere. Two CLI tests failed on Windows and passed on Linux and macOS. Added `eavery-core::paths` (`canonicalize`, `canonical_or_self`, `nearest_existing`, `is_inside`) over the `dunce` crate, and routed the ACP layer and the CLI through it. The session cwd is normalised once, in `open_session`, because that is the path the engine is handed and joins onto for the rest of the session. M4-T02's `policy::is_inside` should call `paths::is_inside` rather than reimplement it. |
| 2026-09-02 | M0-T06 | The `agent-client-protocol` 2.x SDK is not usable here, so the documented §7 fallback was taken. D3 names that fallback, so this is not a change to a locked decision. | Four reasons, any one of them sufficient: (1) the SDK pins `agent-client-protocol-schema` at `=1.5.0`, so the workspace cannot also depend on the `1` line the plan asks for — cargo refuses to resolve; (2) it is built on `async-io` / `async-process` / `blocking` (smol) with no tokio, and the plan puts the rest of the workspace on tokio; (3) `AcpAgentConfig` has command, args and env but no working directory, which `04 §4` requires; (4) its stderr capture is internal and delivered only on exit, so nothing can read the ring while the engine is alive, which M1-T08 needs. Both crates stay in the workspace manifest for a later revisit, with the schema crate pinned to `=1.5.0` so the two can co-resolve if anyone adds them. |
| 2026-09-02 | M0-T06 | `PermissionView.request_id` is documented as "the JSON-RPC id as string" | It carries the tool call id instead. The JSON-RPC id is consumed by the dispatcher before the handler runs, and the tool call id is what the engine, the audit log and the UI all key on. Revisit if M4 needs to correlate two permission requests for one tool call. |
| 2026-09-02 | M0-T05 | The fake-agent script format in `11-testing-ci.md` §2 has no way to exercise the client's `fs/read_text_file` handler, and no way to script a mode change | Added two actions: `fs_read` (`{path, expectRefused}`) and `mode`. `fs_read` is what M4-T03 needs to test D15 — reads served from outside the Project — and `mode` lets a script prove the client reacts to `current_mode_update`. Also added an optional `protocolVersion` to the `initialize` section so a version-mismatch path can be tested. The script parser uses `deny_unknown_fields` throughout: a typo in an action name fails at load time rather than silently replaying a shorter script than the test author wrote. |
| 2026-09-02 | M0-T01 | Several crate versions in `03-architecture.md` §2 do not exist or are stale on crates.io | Pinned what crates.io serves: `git2` 0.21 (plan said 0.20), `rusqlite` 0.40 (0.32), `rmcp` 3.2 (0.8), `ts-rs` 12 (10), `which` 8 (7), `keyring` 4 (3), `calamine` 0.36 (0.26), `umya-spreadsheet` 3.1 (2), `rust_xlsxwriter` 0.99 (0.80), `pdf-extract` 0.12 (0.8), `lopdf` 0.44 (0.34), `quick-xml` 0.42 (0.37), `zip` 8 (2). `agent-client-protocol` 2.0.0 and `agent-client-protocol-schema` 1.7.0 are as planned. `fix-path-env` is not in the workspace manifest yet; it arrives with M1-T02. |
| 2026-09-01 | (review) | `REVIEW-2026-09.md` found spec bugs and inconsistencies across the plan | Applied: D15 (reads allowed outside Project), D16 (checkpoint before restore), `PlanJson` wire struct, `Engine` trait on `&self`, `plan_mode_hint` + plan-mode-exit refusal, `@agentclientprotocol/codex-acp` (Zed package deprecated), `agent-client-protocol-schema = "1"`, Codex CLI + codex-acp download and in-app `codex login`, shallow health check by default, exclude list additions, cloud-placeholder guard, C11–C13 added (prompt injection, Journal growth, concurrency), M5/M6 exit tests swapped, S0 spikes added, vocab lint restricted to strings, `opener` plugin, Ctrl+Z scoping, message-coalesced resume summary. |
